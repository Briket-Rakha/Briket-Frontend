// Import Library
import React from 'react';
import { Grid } from '@material-ui/core';

const Landing = (props) => {
  return (
    <Grid container>
      <h1>TEST LANDING</h1>
    </Grid>
  );
};

export default Landing;
